import { SidebarDropDownDirective } from './sidebar-drop-down.directive';

describe('SidebarDropDownDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarDropDownDirective();
    expect(directive).toBeTruthy();
  });
});
